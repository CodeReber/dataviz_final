var height = window.innerHeight;
var width = window.innerWidth;

var bg = d3.select("bgimg-1");

// bg.attr("width", `${width}px`).attr("height",`${height}px`);
// console.log(`Width: ${width}px and height: ${height}px`);


